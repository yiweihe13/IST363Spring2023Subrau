import styles from './sectioncta.module.scss';

const SectionCTA = () =>{
    return <section className={styles.sectioncta}>
        Call to actins goes here.
    </section>
}
export default SectionCTA